import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    text1:{
        color: 'black',
        textAlign: "center",
        fontSize: 35,
        marginTop: 10,
        fontWeight: "bold"
    },
    
    
    
});